# Dependencias do gerador de entregaveis. Minimo necessario para o pipeline
# conteudo -> forma -> PDF. Instale com: pip install -r requirements.txt
#
# Por que cada uma (ancora: geracao-de-documentos-e-apresentacoes.md):
weasyprint>=60        # HTML/CSS -> PDF com CSS Paged Media (relatorio e deck)
jinja2>=3.1           # injeta o conteudo nos templates (separa conteudo x forma)
markdown>=3.5         # converte o conteudo Markdown do relatorio em HTML
python-pptx>=0.6      # deck .pptx NATIVO editavel (gerar-pptx.py) - formas/chart nativos

# Para .docx NATIVO (editavel no Word), descomente quando o caminho existir:
# python-docx>=1.1     # documento .docx via estilos/template

# Lint: gate de estilo aplicado pelo check.sh (config em pyproject.toml). No CI
# garante que o `ruff check` rode de verdade; local degrada se ausente.
ruff>=0.5
